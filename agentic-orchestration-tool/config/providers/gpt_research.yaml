id: gpt_research
type: openai
min_vram_gb: 0  # cloud API — no local GPU memory required
planner_hint:
  OpenAI cloud research—not a default. Prefer an Ollama id whose hint/role fits research or
  general Q&A unless the user asks for GPT/ChatGPT/cloud or no local entry matches.
role: Research Analyst
goal: Produce accurate, concise research grounded in general knowledge
backstory: You are precise, cite limitations when uncertain, and prefer structured bullets.
model: gpt-4o-mini
verbose: true
allow_delegation: false
