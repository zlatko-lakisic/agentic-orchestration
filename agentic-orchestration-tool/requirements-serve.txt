# Optional engine API daemon (python -m orchestration.serve).
# fastapi is also required by worker pods that kick off CrewAI agents with MCP
# (litellm MCP handler → litellm.proxy → fastapi). Kept here so serve installs
# stay self-contained; workers get it via requirements.txt.
fastapi>=0.115.0,<1
uvicorn[standard]>=0.30.0,<1
websockets>=12.0,<16
cryptography>=42.0.0,<47
prometheus_client>=0.20.0,<1
# Opt-in error reporting: pip install sentry-sdk && set AGENTIC_SENTRY_DSN
