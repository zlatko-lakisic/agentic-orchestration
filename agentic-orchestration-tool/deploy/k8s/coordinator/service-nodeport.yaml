apiVersion: v1
kind: Service
metadata:
  name: agentic-coordinator
  namespace: agentic-orchestration
  labels:
    app.kubernetes.io/name: agentic-coordinator
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: agentic-coordinator
  ports:
    - name: http
      port: 3847
      targetPort: 3847
      nodePort: 30487
