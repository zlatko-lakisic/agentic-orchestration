# filesystem_local gateway — workspace subdir on the run-store PVC.
# Set AGENTIC_K8S_MCP_FILESYSTEM_URL=http://agentic-mcp-filesystem.agentic-orchestration.svc.cluster.local:8081/mcp
# Seed the PVC subdir mcp-fs-workspace/ (see scripts/k8s-local-verify.ps1).
apiVersion: v1
kind: Service
metadata:
  name: agentic-mcp-filesystem
  namespace: agentic-orchestration
  labels:
    app.kubernetes.io/name: agentic-mcp-filesystem
spec:
  selector:
    app.kubernetes.io/name: agentic-mcp-filesystem
  ports:
    - name: http
      port: 8081
      targetPort: 8081
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentic-mcp-filesystem
  namespace: agentic-orchestration
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agentic-mcp-filesystem
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agentic-mcp-filesystem
    spec:
      containers:
        - name: gateway
          image: supercorp/supergateway:uvx
          args:
            - --stdio
            - npx -y @modelcontextprotocol/server-filesystem /workspace
            - --port
            - "8081"
            - --outputTransport
            - streamableHttp
            - --protocolVersion
            - "2024-11-05"
          ports:
            - containerPort: 8081
          volumeMounts:
            - name: workspace
              mountPath: /workspace
              subPath: mcp-fs-workspace
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: agentic-run-store
