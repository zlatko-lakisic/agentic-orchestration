# CI kind cluster — bind-mount host run store (Linux: /tmp/agentic-run-store).
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraMounts:
      - hostPath: /tmp/agentic-run-store
        containerPath: /run/store
