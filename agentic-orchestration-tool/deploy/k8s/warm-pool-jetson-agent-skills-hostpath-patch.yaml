# Mount git-synced Jetson-only agent skills into warm-pool workers (no docker rebuild).
spec:
  template:
    spec:
      containers:
        - name: worker
          volumeMounts:
            - name: jetson-agent-skills
              mountPath: /app/config/agent_skills_jetson
              readOnly: true
      volumes:
        - name: jetson-agent-skills
          hostPath:
            path: /var/projects/agentic-orchestration/agentic-orchestration-tool/config/agent_skills_jetson
            type: DirectoryOrCreate
