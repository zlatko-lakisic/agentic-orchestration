apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agentic-run-store
  namespace: agentic-orchestration
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: agentic-run-store-nfs
  resources:
    requests:
      storage: 10Gi
